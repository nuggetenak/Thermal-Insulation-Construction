{
  "name": "thermal-insulation-construction",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite",
    "build": "npm run content && tsc -b && vite build",
    "preview": "vite preview",
    "content": "node scripts/parse-taxonomy.mjs && node scripts/build-index.mjs",
    "validate": "node scripts/parse-taxonomy.mjs && node scripts/check-ids.mjs && node scripts/validate-content.mjs",
    "check": "npm run validate && npm test && npm run content && tsc -b && vite build",
    "test": "node scripts/test-guardrails.mjs"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-router-dom": "^7.9.1"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.13",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^5.0.0",
    "tailwindcss": "^4.1.13",
    "typescript": "^5.9.2",
    "vite": "^7.1.0"
  }
}
